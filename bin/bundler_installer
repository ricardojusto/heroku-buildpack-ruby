#!/usr/bin/env bash
set -e

BUILD_DIR=$1
RUBY_PATH=$2/bin

echo "-----> Installing Bundler 1.17.3"

# Use the custom Ruby to install Bundler
export PATH="$RUBY_PATH:$PATH"
gem install bundler -v 1.17.3 --no-document

echo "-----> Bundler 1.17.3 installed"